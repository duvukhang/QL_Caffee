# ===================================================================
# 1. Database demo tu sinh bang JPA/Hibernate
# ===================================================================
spring.datasource.url=jdbc:sqlserver://127.0.0.1:1433;databaseName=StoreManagement1;encrypt=true;trustServerCertificate=true;
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:04022005}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Schema phu duoc tao trong Program truoc khi Hibernate update bang.
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# ===================================================================
# 2. Bao mat JWT/form-login
# ===================================================================
jwt.secret=${JWT_secret:Chuoi_Bi_Mat_Sieu_Bao_Mat_Cua_Khang_HUIT_2026_Nhe_Ban_Oi}
jwt.issuer=${Jwt_Issuer:StoreManagement1Backend}
jwt.audience=${Jwt_Audience:StoreManagement1Frontend}

server.port=8088
